---
import ToolLayout from '../layouts/ToolLayout.astro';
import { bioCategories } from '../data/aesthetic-bio.js';

const categoriesJson = JSON.stringify(bioCategories);
---

<ToolLayout title="Aesthetic Bio Ideas" description="Ready-to-copy aesthetic bio templates for Instagram, TikTok, Discord, and more. Cute, cool, edgy, gamer, minimalist &amp; creative bio ideas. Click to copy.">
  <div slot="tool">
    <!-- Platform Compatibility -->
    <div class="mb-6 p-4 rounded-xl border border-[var(--border-color)] bg-[var(--bg-card)]">
      <h3 class="text-sm font-semibold text-[var(--text-primary)] mb-2" data-i18n="compatibility">Platform Compatibility</h3>
      <div class="flex flex-wrap gap-2 text-xs">
        <span class="inline-flex items-center gap-1 px-2 py-0.5 rounded-full bg-emerald-100 dark:bg-emerald-900/30 text-emerald-700 dark:text-emerald-400" data-i18n="platform.instagram">✓ Instagram</span>
        <span class="inline-flex items-center gap-1 px-2 py-0.5 rounded-full bg-emerald-100 dark:bg-emerald-900/30 text-emerald-700 dark:text-emerald-400" data-i18n="platform.tiktok">✓ TikTok</span>
        <span class="inline-flex items-center gap-1 px-2 py-0.5 rounded-full bg-emerald-100 dark:bg-emerald-900/30 text-emerald-700 dark:text-emerald-400" data-i18n="platform.discord">✓ Discord</span>
        <span class="inline-flex items-center gap-1 px-2 py-0.5 rounded-full bg-emerald-100 dark:bg-emerald-900/30 text-emerald-700 dark:text-emerald-400" data-i18n="platform.twitter">✓ Twitter/X</span>
      </div>
      <p class="text-xs text-[var(--text-secondary)] mt-2" data-i18n="aestheticBio.compatNote">All bio templates are text-based and work on every platform. Click any bio to copy it, then paste directly into your profile. Mix and match lines to create your own unique bio!</p>
    </div>

    <!-- Category Tabs -->
    <div id="bio-tabs" role="tablist" class="flex gap-2 overflow-x-auto pb-3 mb-6 scrollbar-hide">
      {bioCategories.map((cat, i) => (
        <button
          class:list={[
            'bio-tab shrink-0 px-4 py-2 rounded-full text-sm font-medium border transition-all whitespace-nowrap',
            i === 0
              ? 'bg-brand-500 text-white border-brand-500 dark:bg-brand-400 dark:text-gray-900'
              : 'border-[var(--border-color)] text-[var(--text-secondary)] hover:border-brand-300 dark:hover:border-brand-600',
          ]}
          data-category={cat.id}
          role="tab"
          aria-selected={i === 0 ? 'true' : 'false'}
        >
          {cat.icon} {cat.name}
        </button>
      ))}
    </div>

    <!-- Bio Grid -->
    <div id="bio-grid" class="grid grid-cols-1 sm:grid-cols-2 gap-3">
    </div>

    <!-- Tips -->
    <div class="mt-8 p-4 rounded-xl border border-[var(--border-color)] bg-[var(--bg-card)]">
      <h3 class="text-sm font-semibold text-[var(--text-primary)] mb-2" data-i18n="aestheticBio.tips">Bio Writing Tips</h3>
      <ul class="text-xs text-[var(--text-secondary)] space-y-1.5 list-disc pl-4">
        <li data-i18n="aestheticBio.tip1">Keep it under the character limit: Instagram 150, TikTok 80, Discord 190.</li>
        <li data-i18n="aestheticBio.tip2">Use line breaks (press Enter) to organize different sections of your bio.</li>
        <li data-i18n="aestheticBio.tip3">Mix text with symbols, emojis, and dividers for visual appeal.</li>
        <li data-i18n="aestheticBio.tip4">Add a call-to-action (CTA) like a link or "DM for collabs" at the end.</li>
        <li data-i18n="aestheticBio.tip5">Update your bio regularly to keep it fresh and relevant.</li>
      </ul>
    </div>
  </div>
</ToolLayout>

<script is:inline src="/scripts/tabs.js"></script>
<script define:vars={{ categoriesJson }}>
  const categories = JSON.parse(categoriesJson);
  const grid = document.getElementById('bio-grid');
  let activeCategory = categories[0]?.id || 'cute';

  function renderCategory(catId) {
    const cat = categories.find(c => c.id === catId);
    if (!cat) return;

    grid.innerHTML = '';
    const fragment = document.createDocumentFragment();
    for (const bio of cat.bios) {
      const card = document.createElement('div');
      card.className = 'tool-card p-4 group relative';

      const pre = document.createElement('pre');
      pre.className = 'text-sm text-[var(--text-primary)] whitespace-pre-wrap font-sans leading-relaxed mb-3';
      pre.textContent = bio;

      const actions = document.createElement('div');
      actions.className = 'flex items-center justify-between opacity-0 group-hover:opacity-100 transition-opacity';

      const copyBtn = document.createElement('button');
      copyBtn.className = 'text-xs px-3 py-1.5 rounded-lg bg-brand-500 text-white hover:bg-brand-600 dark:bg-brand-400 dark:text-gray-900 dark:hover:bg-brand-300 transition-all';
      copyBtn.textContent = 'Copy Bio';
      copyBtn.setAttribute('data-i18n', 'copyBio');
      copyBtn.dataset.copy = bio;

      const favBtn = document.createElement('button');
      favBtn.className = 'text-sm cursor-pointer hover:scale-125 transition-transform';
      favBtn.textContent = '⭐';
      favBtn.dataset.fav = bio;
      favBtn.title = 'Toggle favorite';

      actions.appendChild(copyBtn);
      actions.appendChild(favBtn);

      card.appendChild(pre);
      card.appendChild(actions);
      fragment.appendChild(card);
    }
    grid.appendChild(fragment);
  }

  // Event delegation
  grid.addEventListener('click', (e) => {
    const favBtn = e.target.closest('[data-fav]');
    if (favBtn) {
      e.stopPropagation();
      if (window.toggleFavorite) {
        const isFav = window.toggleFavorite(favBtn.getAttribute('data-fav'));
        favBtn.textContent = isFav ? '🌟' : '⭐';
      }
      return;
    }
    const copyBtn = e.target.closest('[data-copy]');
    if (copyBtn && window.copyToClipboard) {
      window.copyToClipboard(copyBtn.dataset.copy);
    }
  });

  setupCategoryTabs('.bio-tab', (category) => {
    activeCategory = category;
    renderCategory(category);
  });

  renderCategory(activeCategory);
</script>
