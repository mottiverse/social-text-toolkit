---
import ToolLayout from '../layouts/ToolLayout.astro';
import { kaomojiCategories } from '../data/kaomoji.js';

const categoriesJson = JSON.stringify(kaomojiCategories);
---

<ToolLayout title="Kaomoji Collection" description="500+ Japanese text emoticons (kaomoji) organized by emotion. Happy, sad, love, angry, cute & more. Click to copy instantly.">
  <div slot="tool">
    <div class="mb-6 p-4 rounded-xl border border-[var(--border-color)] bg-[var(--bg-card)]">
      <h3 class="text-sm font-semibold text-[var(--text-primary)] mb-2">Platform Compatibility</h3>
      <div class="flex flex-wrap gap-2 text-xs">
        <span class="inline-flex items-center gap-1 px-2 py-0.5 rounded-full bg-emerald-100 dark:bg-emerald-900/30 text-emerald-700 dark:text-emerald-400">✓ Instagram</span>
        <span class="inline-flex items-center gap-1 px-2 py-0.5 rounded-full bg-emerald-100 dark:bg-emerald-900/30 text-emerald-700 dark:text-emerald-400">✓ TikTok</span>
        <span class="inline-flex items-center gap-1 px-2 py-0.5 rounded-full bg-emerald-100 dark:bg-emerald-900/30 text-emerald-700 dark:text-emerald-400">✓ Discord</span>
        <span class="inline-flex items-center gap-1 px-2 py-0.5 rounded-full bg-emerald-100 dark:bg-emerald-900/30 text-emerald-700 dark:text-emerald-400">✓ Twitter/X</span>
        <span class="inline-flex items-center gap-1 px-2 py-0.5 rounded-full bg-amber-100 dark:bg-amber-900/30 text-amber-700 dark:text-amber-400">⚠ SMS</span>
      </div>
      <p class="text-xs text-[var(--text-secondary)] mt-2">Kaomoji use standard Unicode characters and work on all social platforms. Some complex kaomoji may wrap incorrectly in SMS or plain-text emails. Click any kaomoji to copy it instantly.</p>
    </div>
    <!-- Category Tabs -->
    <div id="kaomoji-tabs" class="flex gap-2 overflow-x-auto pb-3 mb-6 scrollbar-hide">
      {kaomojiCategories.map((cat, i) => (
        <button
          class:list={[
            'kao-tab shrink-0 px-4 py-2 rounded-full text-sm font-medium border transition-all whitespace-nowrap',
            i === 0
              ? 'bg-brand-500 text-white border-brand-500 dark:bg-brand-400 dark:text-gray-900'
              : 'border-[var(--border-color)] text-[var(--text-secondary)] hover:border-brand-300 dark:hover:border-brand-600',
          ]}
          data-category={cat.id}
        >
          {cat.icon} {cat.name}
        </button>
      ))}
    </div>

    <!-- Kaomoji Grid -->
    <div id="kaomoji-grid" class="grid grid-cols-2 sm:grid-cols-3 md:grid-cols-4 gap-2">
    </div>

    <!-- Data store -->
  </div>
</ToolLayout>

<script is:inline src="/scripts/tabs.js"></script>
<script define:vars={{ categoriesJson }}>
  const categories = JSON.parse(categoriesJson);
  const grid = document.getElementById('kaomoji-grid');
  let activeCategory = categories[0]?.id || 'happy';

  function renderCategory(catId) {
    const cat = categories.find(c => c.id === catId);
    if (!cat) return;
    grid.innerHTML = '';
    for (const kao of cat.kaomoji) {
      const btn = document.createElement('button');
      btn.className = 'tool-card text-center py-4 hover:border-brand-300 dark:hover:border-brand-600 group';
      btn.innerHTML = '<span class="text-xl block mb-1">' + kao + '</span><span class="text-xs text-[var(--text-secondary)] opacity-0 group-hover:opacity-100 transition-opacity">Click to copy</span>';
      btn.onclick = function() { copyToClipboard(kao); };
      grid.appendChild(btn);
    }
  }

  setupCategoryTabs('.kao-tab', (category) => {
    activeCategory = category;
    renderCategory(category);
  });

  renderCategory(activeCategory);
</script>
