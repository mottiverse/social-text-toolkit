---
import ToolLayout from '../layouts/ToolLayout.astro';
import { emojiComboCategories } from '../data/emoji-combos.js';

const categoriesJson = JSON.stringify(emojiComboCategories);
---

<ToolLayout title="Emoji Combos" description="Pre-made emoji combinations for your social media bio, captions, and messages. Aesthetic, love, gaming, nature themes &amp; more. Click to copy instantly.">
  <div slot="tool">
    <!-- Platform Compatibility -->
    <div class="mb-6 p-4 rounded-xl border border-[var(--border-color)] bg-[var(--bg-card)]">
      <h3 class="text-sm font-semibold text-[var(--text-primary)] mb-2" data-i18n="compatibility">Platform Compatibility</h3>
      <div class="flex flex-wrap gap-2 text-xs">
        <span class="inline-flex items-center gap-1 px-2 py-0.5 rounded-full bg-emerald-100 dark:bg-emerald-900/30 text-emerald-700 dark:text-emerald-400" data-i18n="platform.instagram">✓ Instagram</span>
        <span class="inline-flex items-center gap-1 px-2 py-0.5 rounded-full bg-emerald-100 dark:bg-emerald-900/30 text-emerald-700 dark:text-emerald-400" data-i18n="platform.tiktok">✓ TikTok</span>
        <span class="inline-flex items-center gap-1 px-2 py-0.5 rounded-full bg-emerald-100 dark:bg-emerald-900/30 text-emerald-700 dark:text-emerald-400" data-i18n="platform.discord">✓ Discord</span>
        <span class="inline-flex items-center gap-1 px-2 py-0.5 rounded-full bg-emerald-100 dark:bg-emerald-900/30 text-emerald-700 dark:text-emerald-400" data-i18n="platform.twitter">✓ Twitter/X</span>
        <span class="inline-flex items-center gap-1 px-2 py-0.5 rounded-full bg-emerald-100 dark:bg-emerald-900/30 text-emerald-700 dark:text-emerald-400" data-i18n="platform.all">✓ All platforms</span>
      </div>
      <p class="text-xs text-[var(--text-secondary)] mt-2" data-i18n="emojiCombos.compatNote">Emoji combos use standard emoji characters and work everywhere emojis are supported. Click any combo to copy all emojis at once.</p>
    </div>

    <!-- Search -->
    <div class="mb-6">
      <div class="relative">
        <input
          id="combo-search"
          type="text"
          placeholder="Search emoji combos..."
          data-i18n-placeholder="emojiCombos.searchPlaceholder"
          class="w-full pl-10 pr-4 py-2.5 rounded-xl border border-[var(--border-color)] bg-[var(--bg-card)] text-[var(--text-primary)] placeholder:text-[var(--text-secondary)] focus:outline-none focus:ring-2 focus:ring-brand-500 dark:focus:ring-brand-400 text-sm transition-all"
        />
        <svg class="absolute left-3 top-1/2 -translate-y-1/2 w-4 h-4 text-[var(--text-secondary)]" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M21 21l-6-6m2-5a7 7 0 11-14 0 7 7 0 0114 0z"/></svg>
      </div>
    </div>

    <!-- Category Tabs -->
    <div id="combo-tabs" role="tablist" class="flex gap-2 overflow-x-auto pb-3 mb-6 scrollbar-hide">
      {emojiComboCategories.map((cat, i) => (
        <button
          class:list={[
            'combo-tab shrink-0 px-4 py-2 rounded-full text-sm font-medium border transition-all whitespace-nowrap',
            i === 0
              ? 'bg-brand-500 text-white border-brand-500 dark:bg-brand-400 dark:text-gray-900'
              : 'border-[var(--border-color)] text-[var(--text-secondary)] hover:border-brand-300 dark:hover:border-brand-600',
          ]}
          data-category={cat.id}
          role="tab"
          aria-selected={i === 0 ? 'true' : 'false'}
        >
          {cat.icon} {cat.name}
        </button>
      ))}
    </div>

    <!-- Combo Grid -->
    <div id="combo-grid" class="grid grid-cols-1 sm:grid-cols-2 gap-2">
    </div>
  </div>
</ToolLayout>

<script is:inline src="/scripts/tabs.js"></script>
<script define:vars={{ categoriesJson }}>
  const categories = JSON.parse(categoriesJson);
  const grid = document.getElementById('combo-grid');
  const searchInput = document.getElementById('combo-search');
  let activeCategory = categories[0]?.id || 'aesthetic';

  function renderCategory(catId) {
    const cat = categories.find(c => c.id === catId);
    if (!cat) return;
    const query = searchInput.value.toLowerCase().trim();

    grid.innerHTML = '';
    const fragment = document.createDocumentFragment();
    for (const combo of cat.combos) {
      if (query && !combo.toLowerCase().includes(query)) continue;

      const btn = document.createElement('button');
      btn.className = 'tool-card text-center py-4 hover:border-brand-300 dark:hover:border-brand-600 group relative';
      btn.dataset.copy = combo;
      btn.innerHTML = '<span class="text-xl block mb-1">' + combo + '</span><span class="text-xs text-[var(--text-secondary)] opacity-0 group-hover:opacity-100 transition-opacity" data-i18n="clickToCopy">Click to copy</span>' +
        '<span class="absolute top-2 right-2 text-sm cursor-pointer hover:scale-125 transition-transform" data-fav="' + combo.replace(/"/g, '&quot;') + '" title="Toggle favorite">⭐</span>';
      fragment.appendChild(btn);
    }
    grid.appendChild(fragment);

    if (!fragment.children.length) {
      grid.innerHTML = '<p class="col-span-full text-center text-[var(--text-secondary)] py-8" data-i18n="noResults">No emoji combos found</p>';
    }
  }

  // Event delegation for copy and favorites
  grid.addEventListener('click', (e) => {
    const favBtn = e.target.closest('[data-fav]');
    if (favBtn) {
      e.stopPropagation();
      if (window.toggleFavorite) {
        const isFav = window.toggleFavorite(favBtn.getAttribute('data-fav'));
        favBtn.textContent = isFav ? '🌟' : '⭐';
      }
      return;
    }
    const copyBtn = e.target.closest('[data-copy]');
    if (copyBtn && window.copyToClipboard) {
      window.copyToClipboard(copyBtn.dataset.copy);
    }
  });

  // Search
  searchInput.addEventListener('input', () => renderCategory(activeCategory));

  window.setupCategoryTabs('.combo-tab', (category) => {
    activeCategory = category;
    searchInput.value = '';
    renderCategory(category);
  });

  renderCategory(activeCategory);
</script>
